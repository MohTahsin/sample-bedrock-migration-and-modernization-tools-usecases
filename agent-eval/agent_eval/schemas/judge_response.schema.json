{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Judge Response",
  "description": "Schema for parsed judge response from LLM evaluation",
  "type": "object",
  "required": ["verdict", "score", "confidence", "reasoning"],
  "additionalProperties": false,
  "properties": {
    "verdict": {
      "type": "string",
      "description": "Categorical verdict (for categorical rubrics) or textual summary"
    },
    "score": {
      "type": ["number", "string"],
      "description": "Numeric score (for numeric rubrics) or categorical value"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Judge's confidence in this evaluation (0.0 to 1.0)"
    },
    "reasoning": {
      "type": "string",
      "description": "Judge's explanation for the score/verdict"
    }
  }
}
